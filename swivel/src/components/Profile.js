import MainNav from './MainNav'
import SideNav from './SideNav'

const Profile = ( ) => {
  /*
  This is just a standard profile edit / "settings" screen. The biggest issue with this one is that we don't have a design for it
  so we'll have to be make it up as we go
  */
  return (
    <div>
      <MainNav />
      <SideNav />
    </div>
  )
}

export default Profile
